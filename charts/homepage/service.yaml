apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
  annotations:
    dev.orbstack.http-port: "80"
  labels:
    app: homepage
spec:
  type: ClusterIP
  ports:
  - port: 80
    name: http
    targetPort: 3000
  selector:
    app: homepage
