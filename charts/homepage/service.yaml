apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  type: ClusterIP
  ports:
  - port: 80
    name: http
    targetPort: 3000
  selector:
    app: homepage
